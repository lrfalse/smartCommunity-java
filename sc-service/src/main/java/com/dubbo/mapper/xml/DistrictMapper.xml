<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dubbo.mapper.DistrictMapper">
	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
				adcode,	name,	pcode,	location,	level
	</sql>
<select id="queryCity" resultType="com.commons.entity.DistrictEntity" parameterType="ParamDto">
SELECT id,name FROM t_district WHERE level=#{city}
</select>

</mapper>